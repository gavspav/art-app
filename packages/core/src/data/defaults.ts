import { appStateSchema, layerSchema } from '../schema.js';
import type { AppState, Layer } from '../schema.js';

const baseLayer: Layer = layerSchema.parse({
  name: 'Layer 1',
  layerType: 'shape',
  imageSrc: null,
  shapeType: 'polygon',
  numSides: 6,
  wobble: 0.5,
  curviness: 1,
  width: 250,
  height: 250,
  radiusFactor: 0.125,
  radiusFactorX: 0.125,
  radiusFactorY: 0.125,
  noiseAmount: 0.5,
  noiseSpeed: 0.005,
  opacity: 0.8,
  blendMode: 'source-over',
  colors: ['#0000FF'],
  selectedColor: 0,
  movementStyle: 'bounce',
  movementSpeed: 1,
  movementAngle: 45,
  scaleSpeed: 0.05,
  scaleMin: 0,
  scaleMax: 1.5,
  spinAngle: 0,
  variation: 0.2,
  variationShape: 0.2,
  variationAnim: 0.2,
  variationColor: 0.2,
  variationPosition: 0.2,
  variationScale: 0,
  seed: 1,
  noiseSeed: 1,
  useGlobalSeed: false,
  freq1: 2,
  freq2: 3,
  freq3: 4,
  baseRadiusFactor: 0.4,
  radiusBump: 0,
  rotation: 0,
  xOffset: 0,
  yOffset: 0,
  orbitCenterX: 0.5,
  orbitCenterY: 0.5,
  orbitAngle: 0,
  orbitRadiusX: 0.15,
  orbitRadiusY: 0.15,
  nodes: null,
  syncNodesToNumSides: true,
  viewBoxMapped: false,
  imageBlur: 0,
  imageBrightness: 100,
  imageContrast: 100,
  imageHue: 0,
  imageSaturation: 100,
  imageDistortion: 0,
  position: {
    centerX: 0.5,
    centerY: 0.5,
    x: 0.5,
    y: 0.5,
    vx: 0,
    vy: 0,
    scale: 1,
    scaleDirection: 1,
  },
  visible: true,
  vary: {
    numSides: true,
    curviness: true,
    wobble: true,
    noiseAmount: true,
    radiusFactor: true,
    radiusFactorX: true,
    radiusFactorY: true,
    xOffset: true,
    yOffset: true,
    width: true,
    height: true,
    opacity: false,
    movementStyle: true,
    movementSpeed: true,
    movementAngle: true,
    scaleSpeed: true,
    scaleMin: false,
    scaleMax: false,
    orbitCenterX: false,
    orbitCenterY: false,
    orbitRadiusX: false,
    orbitRadiusY: false,
    freq1: false,
    freq2: false,
    freq3: false,
    imageBlur: true,
    imageBrightness: true,
    imageContrast: true,
    imageHue: true,
    imageSaturation: true,
    imageDistortion: true,
    colors: true,
    numColors: true,
    colorFadeEnabled: true,
    colorFadeSpeed: true,
  },
});

export const DEFAULT_LAYER: Layer = { ...baseLayer };

const baseAppState = appStateSchema.parse({
  isFrozen: false,
  backgroundColor: '#111111',
  backgroundImage: { src: null, opacity: 1, fit: 'cover', enabled: false },
  globalBlendMode: 'source-over',
  globalSeed: 1,
  globalSpeedMultiplier: 1,
  layers: [{ ...DEFAULT_LAYER, id: 'layer-template' }],
  selectedLayerIndex: 0,
  isOverlayVisible: true,
  isNodeEditMode: false,
  classicMode: false,
  zIgnore: false,
  randomizePalette: true,
  randomizeNumColors: true,
  colorFadeWhileFrozen: true,
  syncLayerColorsToFirst: false,
  parameterTargetMode: 'individual',
  showLayerOutlines: false,
  selectedLayerIds: [],
  layerGroups: [],
  editTarget: { type: 'single' },
  quickPreset: null,
  applyVariationInstantly: true,
  morphEnabled: false,
  morphRoute: [1, 2],
  morphDurationPerLeg: 5,
  morphEasing: 'linear',
  morphLoopMode: 'loop',
  morphMode: 'tween',
});

export const DEFAULT_APP_STATE: AppState = { ...baseAppState };

export const CORE_VERSION = '0.1.0';
